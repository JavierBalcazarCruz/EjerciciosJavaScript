const producto = 'Monitor 20 pulgadas';
//Tamaño de un String
console.log(producto.length);

//Retorna la posicion de la primera coincidencia 
console.log(producto.indexOf('pulgadas'));

//Retorna un true si existe  una coincidencia de caso contrario retorna false
console.log(producto.includes('Monitor'));